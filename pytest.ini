[pytest]
python_files =
    django_cloudwatch/*.py
    tests/*.py

addopts =
    --cov django_cloudwatch
    --cov-report term-missing
    tests

pep8ignore =
    *.py W391
    docs/*.py ALL

flakes-ignore =
    docs/*.py ALL

DJANGO_SETTINGS_MODULE=tests.settings

